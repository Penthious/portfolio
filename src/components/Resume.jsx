import React from 'react';
import PropTypes from 'prop-types';

const Resume = (props) => {
	return (
		<div>Resume</div>
	);
};

Resume.propTypes = {};
Resume.defaultProps = {};

export default Resume;
